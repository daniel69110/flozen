{% extends 'base.html.twig' %}

{% block body %}

	<div class="w-auto h-auto relative flex">
		<div class="relative w-max">
			<img src="{{ asset('picture/banner.png') }}" alt="" class="w-max">
		</div>

		<div class="h-auto w-2/5 absolute left-20 top-1/3 flex flex-col">
			<p id="home_title" class="max-md:hidden">"Prenez un instant pour vous reconnecter avec vous-même et retrouver l'harmonie intérieure, parce que votre bien-être mérite toute votre attention"</p>
			
			<a href="{{path('calendar')}}">
				<button id="reservation_button" class="flex justify-center items-center transition duration-300 py-2 px-4 rounded">
					<p class="font-sans text-white text-xs sm:text-sm">Réservez votre séance</p>
				</button>
			</a>
		</div>

	</div>

	<div class="bg-white w-auto p-6">
		<div class="bg-gray-600 h-96 w-auto"></div>
	</div>


	<div class="bg-white w-auto p-6">
		<div class="bg-gray-600 h-96 w-auto"></div>
	</div>

{% endblock %}
