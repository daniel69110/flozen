{% extends 'user_interface/base.html.twig' %}

{% block availability_list %}

	<table class="min-w-full bg-white border border-gray-300 rounded-lg shadow-md ml-3">
		<thead class="bg-gray-100 sticky top-0 z-10">
			<tr class="border-b">
				<th class="py-4 px-4 text-left text-gray-700 font-semibold">Début</th>
				<th class="py-4 px-4 text-left text-gray-700 font-semibold">Fin</th>
				<th class="py-4 px-4 text-left text-gray-700 font-semibold">Action</th>
			</tr>
		</thead>
		<tbody>
			{% for availability in availabilities %}
				<tr class="border-b hover:bg-gray-50">
					<td class="h-20 py-2 px-4">{{ availability.startDateTime|date('Y-m-d H:i') }}</td>
					<td class="h-20 py-2 px-4">{{ availability.endDateTime|date('Y-m-d H:i') }}</td>
					<td class="h- py-2 px-4">
						<a href="{{ path('availability_delete', { id: availability.id }) }}" class="text-red-500 hover:underline">Supprimer</a>
					</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>

{% endblock %}
